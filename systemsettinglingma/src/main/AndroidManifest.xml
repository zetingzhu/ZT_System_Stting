<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZT_System_Stting">

        <!-- 确保 LocaleSettingsActivity 和 ThemeSettingsActivity 正确注册 -->
        <activity android:name=".LocaleSettingsActivity" />
        <activity android:name=".ThemeSettingsActivity" />

        <!-- 为 MainActivityLingma 添加 android:exported 属性 -->
        <activity
            android:name=".MainActivityLingma"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>